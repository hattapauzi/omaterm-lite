install ripgrep for all by default

# Make a 2 options for users options of choice upon installs is it for:

* Desktop use
- Desktop user can open terminal of their choice with plain zsh launched by default, tmux is on demand.

* Server use
- Server user would open their server with bash by default, and user can enter tmux and use zsh there. 
